// write program to convert negative to positive and negative to positive

#include<stdio.h>
int main()
{
    int number;

    printf("enter number:");
    scanf("%d",&number);

    printf("%d",number * -1);


}